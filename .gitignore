# Rust
/target/
**/*.rs.bk
*.pdb
# Note: Cargo.lock is committed for applications to ensure reproducible builds
# It should only be ignored for libraries

# WASM build artifacts (generated by wasm-bindgen)
*.wasm
open_miami.js
open_miami_bg.wasm.d.ts

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Claude
.claude/worktrees

# Session screenshots (generated, not part of the project)
/shots/

# screenshot crops dropped in the root for analysis (perf-trace .json files
# stay VISIBLE as untracked on purpose — review them before cleaning up)
/*.png

# local editor write token for serve.py (never commit)
.editor-token
__pycache__/
# reference audio / inspirations for analysis only — local, never shipped, never committed
.audio-ref/
inspirations/
